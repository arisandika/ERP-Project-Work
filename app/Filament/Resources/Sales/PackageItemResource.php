<?php

namespace App\Filament\Resources\Sales;

use App\Filament\Resources\Sales\PackageItemResource\Pages;
use App\Filament\Resources\Sales\PackageItemResource\RelationManagers;
use App\Models\Sales\PackageItem;
use Filament\Forms;
use Filament\Forms\Form;
use Filament\Resources\Resource;
use Filament\Tables;
use Filament\Tables\Table;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Database\Eloquent\SoftDeletingScope;

class PackageItemResource extends Resource
{
    protected static ?string $model = PackageItem::class;

    protected static ?string $navigationIcon = 'heroicon-o-rectangle-stack';

    protected static bool $shouldRegisterNavigation = false;

    public static function form(Form $form): Form
    {
        return $form
            ->schema([
                //
            ]);
    }

    public static function table(Table $table): Table
    {
        return $table
            ->columns([
                //
            ])
            ->filters([
                //
            ])
            ->actions([
                Tables\Actions\EditAction::make(),
            ])
            ->bulkActions([
                Tables\Actions\BulkActionGroup::make([
                    Tables\Actions\DeleteBulkAction::make(),
                ]),
            ]);
    }

    public static function getRelations(): array
    {
        return [
            //
        ];
    }

    public static function getPages(): array
    {
        return [
            'index' => Pages\ListPackageItems::route('/'),
            'create' => Pages\CreatePackageItem::route('/create'),
            'edit' => Pages\EditPackageItem::route('/{record}/edit'),
        ];
    }
}

